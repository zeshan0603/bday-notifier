import { useState } from "react";

const Filtered = () => {

    const [isActive,setActive] = useState(false);

  return (
    <div>
      I AM FILTERED DIV
    </div>
  )
};

export default Filtered;
